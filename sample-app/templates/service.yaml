apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.name }}
  labels:
    app: {{ .Values.name }}
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: {{ .Values.name }}
